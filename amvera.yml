meta:
  environment: node
  toolchain:
    name: npm
    version: 20

build:
  additionalCommands: npm install --include=dev && npm run build

run:
  command: npm run start
  containerPort: 5000
  persistenceMount: /data
